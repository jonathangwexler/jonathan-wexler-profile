# Optional App Platform specification.
# Replace the GitHub repository placeholder before using this file with doctl or the API.
name: jonathan-wexler-profile
static_sites:
  - name: website
    github:
      repo: YOUR_GITHUB_OWNER/YOUR_REPOSITORY
      branch: main
      deploy_on_push: true
    source_dir: /
    environment_slug: node-js
    build_command: npm run build
    output_dir: dist
    index_document: index.html
